name: PowerShell tests
on: [push, pull_request]
permissions:
  contents: read
jobs:
  test:
    runs-on: windows-latest
    strategy:
      matrix:
        shell: [powershell, pwsh]
    steps:
      - uses: actions/checkout@v4
      - name: Run offline regression tests
        shell: ${{ matrix.shell }}
        run: ./tests/Test-HeliumFixDRM.ps1
